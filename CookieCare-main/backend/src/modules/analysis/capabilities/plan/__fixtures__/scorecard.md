# Running scorecard

| Phase | Manual check performed | Result | Date |
|---|---|---|---|
| Plan-0 | 5 baselines captured, gaps confirmed | PASS — see README.md | 2026-08-29 |
| Plan-1 | Clean build (192 pre-existing errors, unchanged before/after) | PASS | 2026-08-29 |
| Plan-2 | Roles/perspective correct; ambiguous case asks | PASS — see phase2-verify.ts output | 2026-08-29 |
| Plan-3 | Frame mismatch flagged; match unaffected | PASS — see phase3-verify.ts output | 2026-08-29 |
| Plan-4 | Both inventories read correctly | PASS — see phase4-verify.ts output. The under-recall caveat from the first pass is fixed: extractText.ts now preserves docx paragraph/heading boundaries (mammoth convertToHtml instead of extractRawText), segment-document.ts now splits mid-line compound-numbered clauses, and build-inventory.ts now segments its own target doc (PLAN runs before ACT's ensureSegmented). DPA inventory now finds 32 real clause types incl. audit_rights, termination, limitation_of_liability, confidentiality; NDA inventory faithfully covers its actual structure. Zero content loss verified (identical word/char counts old vs new extraction) | 2026-08-29 |
| Plan-5 | Proposition list approved (biggest-weaknesses case) | PASS — user approved 2/2 propositions (liability_cap_adequacy, termination_asymmetry); indemnification_scope correctly did not fire (no indemnity clause in doc) | 2026-08-29 |
| Plan-6 | S4 ad hoc proposition specific, not vague | PASS — user approved change-of-control ask; 2 extra spot-checks (audit cost allocation, dispute jurisdiction) both specific/checkable, no vague boilerplate | 2026-08-29 |
| Plan-7 | Playbook propositions faithful to source | PASS — 5 S3 propositions generated, one per playbook position; honest labeling (verbatim vs paraphrased); doc-role heuristic fixed (SELF_REFERENCE_RE, ordering before presentation fallback); Phase 2/3 regressions checked clean | 2026-08-29 |
| Plan-8 | Reasoning sub-propositions well-formed, paired | PASS — "Is termination balanced?" decomposed into Controller rights (side_a) + Processor rights (side_b), linked via compareGroup, each independently investigable with specific proof standards; non-comparison asks correctly unaffected (S2 propositions, no compareGroup); Phase 2/3 regressions clean | 2026-08-29 |
| Plan-9 | Top-3 trim correct | PASS — parseExhaustiveness 8/8 unit tests; "top 2 biggest weaknesses" generated 3 S2 propositions (90,80,70), correctly trimmed to 2 highest-priority (90,80), audit_rights(70) dropped; Phase 2 regression clean | 2026-08-29 |
| Plan-10 | Follow-up triage correct on mock | PASS — all 4 turns of GDPR chain classified correctly against mocked Art 28 locked-fact set: turn 1=full_replan (no locks), turn 2=answerable_from_locks (focus/scope), turn 3=narrow_addition (novel question), turn 4=answerable_from_locks (synthesis/negotiate) | 2026-08-29 |
| Plan-11 | Ambiguous case → ASK; normal case unaffected | PASS — overlapping patterns (two for limitation_of_liability) correctly trigger ASK with options; non-overlapping vendor-risk patterns produce no false positive; 3 S2 propositions generated in non-ambiguous case | 2026-08-29 |
| Plan-12 | Clustering matches your judgment | PASS — 3 propositions correctly clustered: liability→risk_exposure, termination→exit_and_control, audit→data_governance; ready for compounding-risk checks in Part 4 SYNTHESIZE | 2026-08-29 |
| ACT-0 | Baseline + scorecard captured | PASS — real Cisco DPA run via production PacController captured (baseline-cisco-art28-BEFORE.json/.md); 5 named fixtures mapped (3 covered by existing passing LEGACY tests, 1 open gap documented by code inspection [truncated-extract-false-present, deferred to ACT-5/6], 1 real live bug found+proven [static-alias-table-drift.test.ts: PLAN-invented merged id "subject_matter_duration" orphans 2 correctly-present findings, causing all 10 baseline rows to read cannot_determine]); ~20 leftover broken LEGACY test import paths fixed mechanically (TS errors 192→83, real improvement not regression) | 2026-08-29 |
| ACT-1 | NDA requirementId populated; Cisco unchanged | PASS — real NDA run (production PacController): 8/8 user-facing findings stamped, only internal extraction/summary_point findings unstamped (correct); Cisco re-run: 9/9 user-facing findings stamped, same exemption pattern. Found+fixed a real Phase-2 gap blocking the NDA run: party-perspective vocabulary didn't recognize "Disclosing/Receiving Party" (near-universal NDA language) and detectDefinedPair picked up incidental one-off word mentions ("customer"/"supplier" appearing once) over the document's actual defined vocabulary — fixed with a minimum-mention-count threshold. Added runtime enforcement (validateFindings) since a type-level required field would break the post-hoc stamping architecture; 5/5 new tests pass. Note: "Cisco unchanged" verified as stamping-pattern-unchanged, not byte-identical — LEGACY's PLAN LLM invents a different Art 28 id vocabulary every run (further confirms the dual-namespace bug's root cause from ACT-0) | 2026-08-29 |
| ACT-2 | Clean build | PASS — added establishedBy, gapDescription, dependency ({document, whyNeeded}), baselineComparison ({comparison, reason}), structuralNote, remediation (all optional) to RequirementAssessment in models/requirement-assessment.ts; TS errors unchanged (83); test suite unchanged (135/140, same 5 pre-existing failures) | 2026-08-29 |
| ACT-3 | 6 Art 28 proof standards approved | PASS — user approved all 6 (subject_matter, duration, nature_purpose, data_categories, data_subject_categories, controller_obligations_rights) authored in gdpr/skill.config.ts's requirementEvidence; added proofStandard field to RequirementEvidenceProfile (isolate-requirement-evidence.ts) and the duplicate inline type in models/evidence-package.ts; build clean, 135/140 tests unchanged | 2026-08-30 |
| ACT-4 | 5 adversarial VERIFY cases read by hand | PASS — verifyProposition() built standalone (prompts/verify-proposition.ts + capabilities/act/verify-proposition.ts), verdict enum proves/contradicts/related_not_proof/irrelevant per research doc §2.1, deterministic verbatim quote check with downgrade-on-fabrication. All 5 adversarial cases correct: (1) termination language correctly rejected as duration proof, (2) real term clause correctly proven with honest quote, (3) security measures correctly distinguished from confidentiality, (4) "in perpetuity" correctly caught as contradicts not just non-proof, (5) no-duration passage correctly irrelevant with honest empty quote (fabrication-downgrade path not exercised by LLM behaving honestly — code reviewed, mirrors proven findVerbatimSpan/evidenceVerified pattern). Fixed a real gap: file was named *.test.ts, which the node:test glob swept into the automated suite, silently firing a live paid LLM call and non-deterministic pass/fail on every future regression run — renamed to verify-proposition-verify.ts matching the phaseN-verify.ts manual-script convention; not wired into any real ACT run yet (ACT-Phase 5). Build clean (83), suite unchanged (135/140) | 2026-08-30 |
| ACT-5 | 6 particulars pass; other 8 unchanged | PASS — VERIFY wired into evaluate-package.ts, gated purely on data (every requirement in a package has an authored proofStandard), never on a package/regime name. Real Cisco run: duration correctly proven via the actual end-condition clause (not termination noise), controller obligations correctly proven, subject_matter proven; nature_purpose/categories_of_data/categories_of_data_subjects correctly refused (insufficient_evidence) since the DPA only points to unsupplied external Offer Disclosures — matches DoD table exactly, no false positives. 8 mandatory-clause rows confirmed mechanism-unchanged (verifiedByProposition=false, identical STRUCTURAL_JSON code path never touched). Added resolveRecallCandidates (loosened, generic, no role-classification heuristics) to isolate-requirement-evidence.ts; verifiedByProposition marker on Finding; isSupporting in requirement-status-policy.ts tightened to require nli="entailed" but ONLY when verifiedByProposition is set (zero effect elsewhere) — the actual mechanism for "LOCK only promotes proves/contradicts." VERIFY findings bypass groupedResultsToFindings/judgementForResult entirely (that function's "upgrade if quote has substance" heuristic would silently override VERIFY's verdict). Known cost: ~5min wall-clock for 6 particulars at CRITIQUE_CHECKLIST tier — flagged for ACT-Phase 10 budget work, not addressed now. Build clean (83), suite unchanged (135/140) | 2026-08-30 |
| ACT-6 | All 14 rows pass | PASS — authored proofStandard for all 9 mandatory_clauses requirements (art28_3_a-h + art28_4_subprocessor_flow_down); "all requirements have proofStandard" gate now includes this package too, so it auto-routed onto the same VERIFY wiring with zero new code, confirming ACT-Phase 5's data-driven design. All 15 rows (6 particulars + 9 mandatory) internally consistent — no status/nli contradictions (the OLD path had several: art28_3_d, art28_3_h, art28_4 each showed status/nli mismatches or split present+absent_expected pairs). New results correctly refuse 3 particulars the old path used to get wrong: art28_3_c_security (bare pointer to an unsupplied Information Security Exhibit — exactly the "dependency, not proof" case the standard calls out), art28_3_h_audit (third-party certs on request ≠ real audit/inspection right, matching DoD's own "partial is fair" expectation), art28_4_subprocessor_flow_down (liability for subprocessor's acts ≠ same obligations imposed by contract). Verified via targeted script bypassing PLAN's own non-deterministic package selection (act-phase5-verify.ts), not the full stochastic pipeline — noted as an honest scope caveat. Cost: ~12min wall-clock for the 9-requirement package this run (one transient network retry ate ~4.5min of that) — same ACT-Phase 10 budget-work caveat as Phase 5. Build clean (83), suite unchanged (135/140) | 2026-08-30 |
| ACT-7 | Prose depth matches Mastercard bar | PASS — VERIFY output extended with establishedBy/gapDescription/dependency/structuralNote/remediation (baselineComparison deferred — needs proofStandard to carry a second "strong" tier, not authored yet); wired through Finding→aggregate-requirements.ts→RequirementAssessment→analytical-synthesis.ts prompt and render-output.ts's locked table. Real run on all 15 Art 28 rows produced counsel-grade sentences reading like the actual Mastercard example ("specifies the duration... by tying the DPA's term to the underlying Agreement", "fails to describe the nature... though it states the purpose") — user approved 2026-08-30. Synthesis correctly clustered 5 unrelated-cause insufficient-evidence rows into one coherent "Subprocessor and Security Governance Gaps" theme. Found+fixed 3 real bugs while verifying: (1) GAP_CLAIM_RE false-positived on "0 Gap" zero-counts — fixed with negative lookbehind; (2) analytical-synthesis's maxOutputTokens(1200) was too tight for enriched rows, silently truncating JSON and falling back to mechanical rollup — raised to 3000, confirmed real synthesis now succeeds; (3) render-output.ts's table duplicated gapDescription (once via support.claim, once via the enrichment suffix) — fixed by skipping the suffix when base already contains it. Added guardUnsupportedDependencyClaim mirroring the existing gap guard, and extended guardUnsupportedInference to recognize a locked gapDescription as valid grounding for "missing"-shaped language (a real gap this guard had before enrichment existed). Build clean (83), suite 149/154 (same 5 pre-existing failures) | 2026-08-30 |
| ACT-8 | NDA passes; lint green | PASS — authored proofStandard for all 6 NDA requirements (confidentiality_definition incl. mutuality + exclusions, purpose_limitation, return_or_destruction, nlra_section_7 reframed to protective polarity so it fits the same proves=good convention, term_and_survival, governing_law) in skills/doc-types/nda/skill.config.ts — zero capability-file changes, proving VERIFY is genuinely regime/doc-type agnostic. generic-handler-domain-lint.test.ts fixed to green (was failing since ACT-0: 2 pre-existing regime-token comment leaks in resolve-packages.ts + 1 I introduced myself in my own ACT-Phase 5 comment — all 3 fixed, comments only, no logic changes). Real run on the actual mutual NDA: exit-gate scenario confirmed — nda.term_and_survival now Strong/Present with a single coherent sentence covering both the 2-year term and 3-year survival period, no placeholder or status/rationale contradiction. confidentiality_definition correctly flagged missing standard exclusions while crediting the definition's mutuality; nlra_section_7 correctly insufficient_evidence (no employee-facing language in this B2B instrument, honestly reflected rather than forced into a false pass/fail). Build clean (83), suite 150/154 (4 pre-existing failures, down from 5) | 2026-08-30 |
| ACT-9 | AUDIT faster; contradiction caught | PASS — ground-findings.ts's quoteInSource re-check skipped for verifiedByProposition findings (VERIFY's own quoteAppearsIn already gave a stronger, narrower guarantee at construction time — "re-doing grounding work ACT should never have skipped in the first place", research doc §2.1); run-audit.ts's contradiction sweep (maybeAppendVerificationNotes) left completely untouched, per the doc's explicit distinction that it "genuinely needs a second independent pass." Empirically confirmed: 31.9% average wall-clock reduction in groundFindings across 5 trials of 200 findings, zero incorrect downgrades (all verified findings correctly stayed present); injected table-vs-narrative contradiction (memo falsely claims audit rights Present against a locked absent_expected finding) still correctly caught, 3 accurate contradiction notes produced. Fixed a real gap found along the way: ground-findings.test.ts had 6 broken import paths left over from the LEGACY_CAPABILITIES move (never fixed since the module split), and capabilities/audit/ground-findings.ts's fresh passthrough stub was missing entirely — both fixed, dropping TS errors from 83 to 76 (real reduction, not a regression). 2 new deterministic tests added (verified-skips-recheck, verified-and-unverified-coexist-correctly). Build clean (76), suite 158/162 (4 pre-existing failures) | 2026-08-30 |
| ACT-10 | Lite = Deep correctness, faster | PASS with one honest caveat — added verifyCandidateCap/verifySkipSupportingPriority to AnalysisProfile; evaluate-package.ts skips PLAN-authored "supporting"-priority requirements entirely under Lite (reuses IntentRequirement.priority, already-existing PLAN field, not new authoring); fixed analytical-synthesis.ts's maxOutputTokens to use the already-existing resolveSynthesisMaxOutputTokens ceiling (lite/deep-aware, report-complexity-aware) instead of my own ACT-Phase-7 hardcoded 3000 — a real bug, since 3000 was actually below the Lite hard cap it should have deferred to; added an explicit prose-length instruction to the synthesis prompt (lite: ≤4 themes/risks, terse; deep: ≤8, full depth) plus matching guardSynthesis caps. CRITICAL FINDING while verifying: initially set verifyCandidateCap to 5 for Lite, then caught empirically on the real NDA that this SILENTLY MISSED the correct return_or_destruction clause (ranked position 6 of 40 by the existing generic scoring heuristic) — a real "identical correctness" violation, not an acceptable tradeoff (research doc §10: "budget as scope, never as rigor"). Reverted candidate cap to 10 for both modes; Lite's scope reduction comes from requirement-skipping alone. Re-verified: return_or_destruction now identical between modes; the only 2 differences are the intentional, transparently-labeled priority-skips (term_and_survival, governing_law — "Not investigated under Lite mode..."); wall-clock reduction 23.7% (honest number, down from an initially-measured but unsafe 70.6%). Caveat: the ~2min target is not hit for this NDA package under the current one-real-LLM-call-per-candidate architecture at full CRITIQUE_CHECKLIST/high thinking (deliberately kept identical in both modes per the no-rigor-reduction principle) — flagging this as a known, accepted tradeoff favoring correctness over the aspirational target, not something papered over. Fixed 2 more broken LEGACY-move import paths (synthesis-ceiling.test.ts, targeted-repair.test.ts) discovered along the way, both with pre-existing dedicated tests for exactly the ceiling logic touched here — all 12 pass. Build clean (69, down from 76), suite 170/174 (4 pre-existing failures) | 2026-08-30 |
| Integration-1 | All 5 fixtures pass end to end | PARTIAL — full production PacController re-run confirmed on the GDPR/Cisco ask specifically: the original Plan-Phase-0 BEFORE baseline showed all 10 assessments falsely cannot_determine (dual-namespace bug); this run shows 5 correctly strong/present, 1 conditional, 4 honestly cannot_determine — a real, differentiated result, not a uniform failure. The other 4 fixed asks (biggest-weaknesses, termination-balanced, playbook-alignment, followup-chain) were not re-run end-to-end this pass due to time — each was already verified individually and repeatedly at the PLAN-phase and ACT-phase level throughout this rebuild (Plan-Phase 5-12, ACT-Phase 5-10 verification scripts). Immediately followed by the full LEGACY_CAPABILITIES → capabilities/ structural merge (see below); re-verified full analysis-module test suite afterward: 373/387 passing, all 14 failures confirmed pre-existing via git-stash comparison against the pre-session commit — zero regressions from either the phase work or the restructure | 2026-08-30 |
| Cleanup | LEGACY_CAPABILITIES merged into capabilities/, folder deleted | PASS — 106 files moved (act/, plan/, reporting/, audit/, critique/, ask/, persist/, plus their __fixtures__), all relative imports rewritten to resolve correctly from the new locations (two-pass: specifier-aware rewrite for moving-file targets, then a corrective pass resolving every import against disk to catch stable-target specifiers whose SOURCE moved). capabilities/index.ts rewritten to import each capability function directly instead of composing a "legacy defaults" object; capabilities/LEGACY_CAPABILITIES/ no longer exists. Found+fixed one hardcoded path.join() string-literal bug the specifier-rewrite couldn't see (generic-handler-domain-lint.test.ts's own ACT_DIR-relative escape paths, off by one level). TS errors 69→35 (all remaining are pre-existing, confirmed via git stash). Test suite unchanged: 373/387, same pre-existing 14 | 2026-08-30 |
