// ─── PlainBackground — Version A ─────────────────────────────────────────────
// A completely flat #F7F8FA background. No video, no gradients, no layers.
// The content is the only visual focus.

export function PlainBackground() {
  return null; // background is set on the root element via inline style
}
